export default function Home() {
  // Landing view. The persistent scene (sea, header, live readout) lives in layout.tsx.
  // Buoys, the first-visit hint, and the index nav arrive in Prompt 4.
  return null;
}
